# Hyprland configuration overrides
# Add your custom settings here

# Example overrides:
# $mainMod = SUPER
$browser = chromium --force-device-scale-factor=1

# Use single default monitor (see all monitors with: hyprctl monitors)
# monitor=,preferred,auto,1.6
#
# # trigger when the switch is turning on
# bindl = , switch:on:Lid Switch, exec, hyprctl keyword monitor "eDP-1, disable"
# # trigger when the switch is turning off
# bindl = , switch:off:Lid Switch, exec, hyprctl keyword monitor "eDP-1, preferred, auto, 1.6"
#
# bind = SUPER SHIFT, D, exec, hyprctl keyword monitor eDP-1, disable
# bind = SUPER SHIFT, F, exec, hyprctl keyword monitor eDP-1, preferred, auto, 1.6
#
# unbind = SUPER, D
# bindd = SUPER, D, Discord, exec, omarchy-launch-webapp "https://discord.com/channels/@me"
# unbind = SUPER, G
bindd = SUPER, N, Notion, exec, omarchy-launch-webapp "https://www.notion.so/"



#On Start
exec-once = sleep 1 && workspace 1 silent && discord
exec-once = sleep 1 && workspace 6 silent && spotify
exec-once = sleep 1 && workspace 3 silent && opera
exec-once = sleep 1 && workspace 7 silent && google-chrome --app=https://web.whatsapp.com
exec-once = sleep 1 && workspace 4 silent && google-chrome --app=https://www.notion.so
exec-once = sleep 1 && workspace 2 silent && alacritty

# Monitors

# DP-2 config
workspace = 2, monitor:DP-2, default:true
workspace = 3, monitor:DP-2
workspace = 4, monitor:DP-2
workspace = 5, monitor:DP-2

# DP-1 config
workspace = 1, monitor:DP-1, default:true
workspace = 6, monitor:DP-1
workspace = 7, monitor:DP-1
workspace = 8, monitor:DP-1
workspace = 9, monitor:DP-1
workspace = 0, monitor:DP-1

windowrulev2 = workspace 6, class:^(Spotify)$


